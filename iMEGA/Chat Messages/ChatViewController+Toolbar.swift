import UIKit

enum ToolbarType {
    case delete
    case forward
}

extension ChatViewController {
    func customToolbar(type: ToolbarType) {
        switch type {
        case .forward:
            
            
            
            break
        case .delete:
            break
        default:
            break
        }
    }
}

