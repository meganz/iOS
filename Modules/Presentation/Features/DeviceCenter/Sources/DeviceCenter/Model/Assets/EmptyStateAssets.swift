public struct EmptyStateAssets {
    public let image: String
    public let title: String
    
    public init(
        image: String,
        title: String
    ) {
        self.image = image
        self.title = title
    }
}
